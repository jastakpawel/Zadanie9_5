var buttonElements = document.getElementsByClassName('button');
var numberOfElements = buttonElements.length;

for (var buttonElements = 0; buttonElements<numberOfElements; buttonElements++) {
	alert(buttonElements.innerText);
}
